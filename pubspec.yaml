name: minify_link
description: This project is a sample for a clean architecture model in my perspective

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  get_it: ^7.2.0
  dio: ^4.0.6
  pretty_dio_logger: ^1.1.1
  dartz: ^0.10.1
  equatable: ^2.0.5
  logger: ^1.1.0
  url_launcher: ^6.1.5

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  mockserver: ^1.4.0
  mockito: ^5.3.0
  build_runner: ^2.2.0

flutter:

  uses-material-design: true